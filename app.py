"""Temporary placeholder app.

The previous Streamlit model UI was intentionally cleared so the repository can
focus on data ingestion first.
"""


def main() -> None:
    print(
        "The previous app was cleared. Use scripts/fetch_ten_seasons.py "
        "to download NBA data exports."
    )


if __name__ == "__main__":
    main()
